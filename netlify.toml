[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "20"

# 단일 페이지: 모든 경로를 index.html로 (함수 경로는 제외)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
